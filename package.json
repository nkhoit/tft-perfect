{
  "name": "tft-trait-explorer",
  "private": true,
  "scripts": {
    "check": "node --check web/search-utils.js && node --check web/saved-searches.js && node --check web/share-state.js && node --check web/team-code.js && node --check web/worker.js && node --check web/app.js",
    "test": "node --test"
  }
}
